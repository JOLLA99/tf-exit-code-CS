# Week 05 - Software Engineering / Design Pattern + Mock Interview

---

## 제출 기준

- 필수 답변: COMMON-161 ~ COMMON-180
- 선택 답변: COMMON-181 ~ COMMON-200

---

## 필수 질문

## [COMMON-161] 객체지향 프로그래밍이 무엇이고, 주요 특징을 설명해 주세요.

답변: 객체는 데이터와 동작을 하나로 묶어 관리하는데, 이런 객체를 조합해서 프로그래밍 하는 걸 객체지향 프로그램이라고 합니다. 객체의 상태를 필드로, 동작을 메서드로 정의합니다.

특징으로는 캡슐화, 상속, 다형성, 추상화 등이 있습니다.

참고 자료:

---

## [COMMON-162] 캡슐화, 상속, 다형성, 추상화에 대해 설명해 주세요.

답변: 

- 캡슐화: 객체의 상태와 동작을 하나로 묶고 접근 제어자로 외부 접근을 제한하는 정보 은닉을 제공합니다.
- 상속: 부모 클래스의 속성과 기능을 자식 클래스가 물려 받아 코드를 재사용하고 확장을 용이하게 합니다.
- 다형성: 하나의 타입으로 여러 객체를 다룰 수 있고, 동일한 메서드를 호출해도 객체 실제 타입에 따라 다르게 동작하는 특성입니다.
- 추상화: 객체의 공통적인 특징을 모델링하는 과정입니다. 세부적인 구현은 숨기고, 인터페이스나 추상 클래스를 통해 역할만 정의합니다.

참고 자료:

---

## [COMMON-163] SOLID 원칙에 대해 설명해 주세요.

답변: 객체지향 설계에서 유지보수성과 확장성을 높이기 위한 다섯 가지 설계 원칙입니다.

- SRP(단일 책임 원칙)는 하나의 클래스는 하나의 책임만 가져야 한다는 원칙,
- OCP(개방-폐쇄 원칙)는 확장은 열려 있고, 변경은 닫혀 있어야 한다는 원칙,
- LSP(리스코프 치환 원칙)는 자식 클래스에서 부모 클래스를 대체할 수 있어야 한다는 원칙,
- ISP(인터페이스 분리 원칙)는 클라이언트가 사용하지 않는 메서드에 의존하지 않도록 인터페이스를 분리해야 한다는 원칙,
- DIP(의존성 역전 원칙)는 구체 구현이 아닌 추상화에 의존해야 한다는 원칙입니다.


참고 자료:

---

## [COMMON-164] 단일 책임 원칙이 무엇이고, 왜 중요한지 설명해 주세요.

답변: 단일 책임 원칙은 하나의 클래스나 모듈이 하나의 책임만 가져야 한다는 원칙입니다.

예를 들어서 주문 서비스가 주문 생성, 결제 처리, 알림 발송, 로그 기록까지 모두 담당하면 책임이 많아집니다.
결제 정책이 바껴도 수정해야 하고, 알림 방식이 바껴도 수정돼야 하는데, 이러면 유지보수나 변경이 어려워질 수 있습니다.

그래서 SRP를 잘 지켜서 코드 응집도를 높이고, 변경에 강한 구조를 만드는 것으로 알고 있습니다.

참고 자료:

---

## [COMMON-165] 의존성 역전 원칙이 무엇이고, 어떤 장점이 있는지 설명해 주세요.

답변: 구체 클래스가 아닌, 인터페이스나 추상 타입에 의존한다는 의미입니다.

예를 들어 주문 서비스가 `KakaoPayClient`에 직접 의존하면 결제 수단이 바뀔 때 주문 서비스 코드도 수정될 수 있는데,
`PaymentClient` 인터페이스에 의존하고 실제 구현체를 `KakaoPayClient`, `CardPaymentClient`로 분리하면 결제 방식이 바껴도 영향을 받지 않습니다.

그래서 계층 간 결합도를 낮추고, 구현체 교체를 쉽게 할 수 있습니다.

참고 자료:

---

## [COMMON-166] 인터페이스를 사용하는 이유와 장단점을 설명해 주세요.

답변: 인터페이스는 객체가 제공해야 하는 기능을 정의하려고 사용합니다. 

구현체 교체가 쉬워지고 기능의 역할을 정의해놓고 사용해서 코드 구조 이해도 쉽습니다.

다만, 인터페이스를 너무 많이 만들면 구조가 오히려 복잡해질 수도 있고, 변경 가능성이 없는데 만들면 오히려 가독성이 떨어지기도 합니다.

참고 자료:

---

## [COMMON-167] 추상 클래스와 인터페이스의 차이에 대해 설명해 주세요.

답변: 

- 추상 클래스는 상태와 동작을 묶어주는 부모 역할
```java

abstract  class Animal {
    String name;
    
    public Animal(String name) {
        this.name = name;
    }
    
    public void sleep() {
        System.out.println(name + "가 잠을 자요.");
    }
    
    // 자식이 직접 구현해야 되는 추상 메서드
    abstract void makeSound();
}

class Dog extends Animal {
    public Dog(String name) {
        super(name);
    }
    
    @Override
    void makeSound() {
        System.out.println("멍멍");
    }
}
```

- 인터페이스는 기능을 정의해두고 활용해서 쓰는 용도이다.
```java
interface Flyable {
    void fly();
}

interface Swimmable {
    void swim();
}

class Duck implements Flyable, Swimmable {
    @Override
    public void fly() {
        System.out.println("오리 날다!");
    }
    
    @Override
    public void swim() {
        System.out.println("오리 수영한다!");
    }
}

class Airplane implements Flyable {
    @Override
    public void fly() {
        System.out.println("비행기가 난다!");
    }
}
```

참고 자료:

---

## [COMMON-168] 디자인 패턴이 무엇이고, 왜 사용하는지 설명해 주세요.

답변: 반복적으로 발생하는 문제를 해결하기 위한 설계 패턴으로, 생성, 구조, 행동 패턴으로 구분합니다.

- 생성: 싱글톤, 추상 팩토리, 팩토리 메서드, 빌더, 프로토타입
- 구조: 어댑터, 컴퍼짓, 데코레이터, 퍼사드, 플라이웨이트, 프록시
- 행동: 커맨드, 인터프리터, 반복자, 중재자, 모멘토, 옵저버, 상태, 전략, 템플릿 메서드

참고 자료:

---

## [COMMON-169] Singleton Pattern이 무엇이고, 장단점은 무엇인지 설명해 주세요.

답변: 특정 클래스의 인스턴스를 하나 생성하고, 그걸 어디서든 접근할 수 있도록 하는 생성 패턴입니다.

전역적으로 하나만 존재해야 되는 객체를 관리할 수 있지만, 객체 간 결합도가 높아질 수 있습니다.
또, 멀티스레드 환경에서는 인스턴스 생성 과정의 동시성 문제도 고려해야 합니다.

참고 자료:

---

## [COMMON-170] Factory Method Pattern이 무엇이고, 어떤 상황에서 사용할 수 있는지 설명해 주세요.

답변: 팩토리 메서드 패턴은 객체 생성 로직을 직접 코드에 두지 않고, 담당 메서드나 하위 클래스에 위임하는 생성 패턴입니다.

객체 생성 로직이 복잡하거나, 구체 클래스에 직접 의존하지 않게 만들고 싶을 때 사용할 수 있습니다.


참고 자료:

---

## [COMMON-171] Strategy Pattern이 무엇이고, 어떤 문제를 해결할 수 있는지 설명해 주세요.

답변: 전략 패턴은 실행 중 교체 가능한 알고리즘이나 정책을 전략 객체로 분리하는 패턴입니다.

예를 들어서 할인 정책이 정률, 정액, 쿠폰으로 나뉜다고 할 때 하나의 메서드 안에서 `if-else`로 다 처리하면 정책이 늘어날 때마다 코드 복잡성이 올라갑니다.
이럴 때 할인 정책을 `DiscountStrategy` 인터페이스로 추상화하고, 각 정책을 별도 구현체로 분리할 수 있습니다.

그래서 새로운 전략이 추가될 때 기존 코드 수정을 별로 하지 않고, 구현체를 추가하는 방식으로 확장할 수 있고, 알고리즘 교체를 유연하게 만듭니다.

참고 자료:

---

## [COMMON-172] Observer Pattern이 무엇이고, 어떤 상황에서 사용할 수 있는지 설명해 주세요.

답변: 옵저버 패턴은 객체의 상태 변화가 발생했을 때, 그 객체에 의존하는 여러 객체들에게 알림을 전달하는 패턴입니다.

예를 들어 주문이 완료되었을 때 재고 차감, 사알림 발송, 포인트 적립 같은 후속 작업이 필요할 수 있는데, 주문 서비스가 이 모든 후속 작업을 직접 호출하면 결합도가 높아집니다.
그래서 옵저버 패턴을 사용해서 주문 완료 이벤트를 발행하고, 관심 있는 객체들이 이런 이벤트를 구독해서 처리할 수 있도록 만듭니다.

그래서 이벤트 알림, UI 이벤트 처리, 발행-구독 구조 등에서 사용하는 것으로 알고 있습니다.

참고 자료:

---

## [COMMON-173] MVC 패턴이 무엇이고, 각 구성 요소의 역할을 설명해 주세요.

답변: 모델, 뷰, 컨트롤러로 나눈 구조입니다.

- 모델은 데이터와 비즈니스 로직을 담당하는 영역으로 Spring 기준으로는 넓게 Entity, Service, Repository 계층이 이 영역에 가깝다고 봅니다.

- 뷰는 말 그대로 사용자에게 보여지는 영역이고 JSP, Thymeleaf 같은 템플릿이 뷰에 해당됩니다.
REST API에서는 당연히 JSON 응답이 일종의 View 표현 역할을 한다고 볼 수 있습니다.

- 컨트롤러는 클라이언트 요청을 받아서 필요한 모델 로직을 호출해서 결과를 뷰나 Response Body로 전달합니다.
이것도 스프링 MVC 기준으로 `DispatcherServlet`이 Front Controller 역할을 하고, 요청을 적절한 Controller로 전달합니다.

참고 자료:

---

## [COMMON-174] Layered Architecture가 무엇이고, 어떤 장단점이 있는지 설명해 주세요.

답변: 애플리케이션을 역할에 따라 계층적으로 나누는 아키텍처입니다.
표현 계층, 애플리케이션 계층, 데이터 계층 등으로 나누는데, 예를 들어 Spring에서 Controller, Service, Repository 구조가 이 계층형 구조와 부합합니다.

계층 별로 역할이 분리돼서 구조 이해가 쉽고, 유지보수나 테스트가 쉽습니다.
다만, 단순한 기능에도 계층을 모두 나눠서 진행하면 코드가 장황해질 수 있습니다.


[보완]

이 레이어 아키텍처는 기본적으로 코드 안에서 역할을 어떻게 나눌 것인지에 대한 논리적 분리이다.
코드 유지보수성 및 재사용성을 높이기 위함.

대표적인 3 Layer 

- 표현 계층: UI, 컨트롤러
- 비즈니스 로직 계층: 서비스 (핵심 비즈니스 로직)
- 데이터 액세스 계층: 리포지토리(DAO - DB랑 통신하는 계층)

직접적인 메서드 호출을 통해 데이터를 주고 받음(네트워크 통신 불필요)

반면, 3-Tier 아키텍처는 인프라(서버)를 어떻게 구성할 것인가에 초점을 맞춘 물리적 분리이다.
보안, 확장성, 서버 부하 분산에 초점을 맞춤.

- 각 티어가 물리적으로 분리된 장비나 독립된 네트워크에 존재

대표적인 3 Tier

- 표현 티어: 웹 서버, 클라이언트
- 애플리케이션 티어: WAS, API 서버
- 데이터 티어: 데이터베이스 서버

반드시 네트워크 통신(HTTP, TCP/IP)을 통해서만 데이터를 주고 받음.

참고 자료:

---

## [COMMON-175] 관심사의 분리가 무엇이고, 좋은 코드 구조에 왜 중요한지 설명해 주세요.

답변: 서로 다른 책임을 가진 코드를 분리하는 설계 원칙입니다.

한 곳에서 요청 검증, 비즈니스 로직, DB 접근, 응답 변환을 전부 넣으면 변경 범위도 커지고, 테스트가 어려워지기 때문에

컨트롤러에 요청/응답, 서비스에 비즈니스 로직, 리포지토리에 데이터 접근을 담당하도록 분리해서 유연한 구조를 갖추곤 합니다.

그래서 관심사 분리는 유지보수, 가독성이나 확장성을 높이는데 중요합니다.

참고 자료:

---

## [COMMON-176] 결합도와 응집도가 무엇이고, 좋은 설계에서는 각각 어떻게 관리되어야 하는지 설명해 주세요.

답변: 결합도는 코드의 어떤 부분을 수정하면 다른 부분이 영향을 받을 가능성이 커지는, 서로 얼마나 의존하는지를 의미합니다.

응집도는 한 클래스 안의 기능이 얼마나 잘 모여있는지를 의미합니다.

그래서 결합도를 낮추고, 응집도를 높여서 모듈이나 클래스가 명확한 책임을 갖되, 다른 클래스랑은 필요한 인터페이스로 느슨하게 연결되도록 설계하는 것이 좋습니다.

**안 좋은 설계 예시 (낮은 응집도, 높은 결합도)**

```java

public class OrderProcessor {
    public void processOrder(Order order) {
        // 1. 재고 처리 (주문 처리 클래스가 DB 접근 방식까지 알아야 됨)
        MysqlDtabase db = new MysqlDatabase(); // 구체적인 DB 클래스에 의존
        db.connect("jdbc:mysql://localhost...");
        db.connect("UPDATE inventory SET stock = stock - 1 WHERE id = " + order.getItemId());
        
        // 2. 결제 처리 (카카오페이 기술에 강력하게 결합)
        KakaoPayAPI kakaoPay = new KakaoPayAPI(); // 구체적인 결제 클래스를 직접 생성함 
        boolean isPaid = kakaoPay.sendMoney(order.getAmount());
        
        // 3. 알림 발송 (이메일 발송 설정까지 여기서 다 함)
        if (osPaid) {
            SmtpMailSender mailSender = new SmtpMailSender();
            mailSender.setHost("smtp.gmail.com");
            mailSender.send(order.getUserEmail(), "주문이 완료되었습니다.");
        }
    }
}

```

- OrderProcessor가 주문 흐름 제어, DB SQL 쿼리 작성, 메일 서버 설정을 한 곳에서 처리하고 있는데, 예를 들어 메일 서버 주소만 바껴도 이 클래스를 수정해야 함 -> 낮은 응집도
- 코드에서 new KakaoPayAPI()를 직접 생성하고 있는데, 네이버페이 같은 방식 추가하려면 기존 코드 뜯어 고쳐야 함 -> 높은 결합도

**좋은 설계 예시(높은 응집도, 낮은 결합도)**

```java
interface InventoryRepository {void decreaseStock(String itemId);}
interface PaymentService {boolean pay(int amount);}
interface NotificationService {void notify(String target, String message);}

public class OrderProcessor {
    
    // 인터페이스만 의존하게 설계
    private final InventoryRepository inventory;
    private final PaymentService payment;
    private final NotificationService notification;
    
    // 주입 받기
    public OrderProcessor(InventoryRepository inventory, PaymentService payment, NotificationService notification) {
        this.inventory = inventory;
        this.payment = payment;
        this.notification = notification;
    }
    
    // 주문의 전체 흐름만 제어 (높은 응집도)
    public void processOrder(Order order) {
        inventory.decreaseStock(order.getItemId());
        
        boolean isPaid = payment.pay(order.getAmount());
        
        if (isPaid) {
            notification.notify(order.getUserEmail(), "주문이 완료되었습니다.");
        }
    }
}
```

- 재고 줄이고 -> 결제하고 -> 알림 보내는 주문의 흐름에만 집중 (높은 응집도)
- 결제 방식 다른 걸 추가해도 코드 수정 불필요 (낮은 결합도)

참고 자료:

---

## [COMMON-177] 테스트 코드가 필요한 이유와 단위 테스트, 통합 테스트의 차이를 설명해 주세요.

답변: 안정성과 유지 보수 때문이라고 생각합니다. 테스트 코드가 있으면 기능 변경이나 리팩토링 할 때 기존 기능이 깨지는지 빠르게 확인이 가능합니다.

그래서 보통 작은 로직이나 클래스 검증 시에 단위 테스트, 컨트롤러부터 서비스, 리포지토리, DB 연동까지의 전체 요청 흐름이 정상인지 볼 땐 통합 테스트를 합니다.

참고 자료:

---

## [COMMON-178] 리팩토링이 무엇이고, 리팩토링 시 주의해야 할 점을 설명해 주세요.

답변: 외부 기능은 바꾸지 않고, 내부 코드 구조를 개선하는 작업입니다. 중복 코드를 제거하거나, 책임을 좀 나눠서 분리하는 작업으로 가독성, 확장성 등을 높이기 위해 합니다.

기존 동작이 달라지지 않으면서 변경하는 것이 중요하고, 한 번에 큰 단위로 수정하기보다는 작은 단위로 변경해서 검증하는 방식이 안전합니다.

참고 자료:

---

## [COMMON-179] 기술 선택 시 trade-off를 설명한다는 것은 어떤 의미인지 설명해 주세요.

답변: 특정 기술 선택 시에 생기는 장단점, 비용 등을 설명하는 것입니다.

Redis 캐시를 도입하면 조회 성능은 개선되지만, 캐시 무효화, 데이터 정합성, 운영 복잡도 같은 게 생기거나,
또 MSA 도입 시에 확장성과 분산 트랜잭션, 모니터링, 네트워크 통신 복잡도가 맞물리는 것처럼 '기술 자체가 좋아서 쓰기'보다는 '현재 상황에 어떤 대안이 있고, 각 기술이 어떤 장점과 비용을 가지기 때문에 선택했다'는 구조로 설명할 수 있어야 합니다.

참고 자료:

---

## [COMMON-180] 본인의 프로젝트에서 기술적 의사결정을 설명할 때 어떤 구조로 답변하면 좋을지 설명해 주세요.

답변: 

문제 상황 및 분석 -> 해결 방안 및 선택 이유 -> 적용 성과 

참고 자료:

---

## 선택 질문

## [COMMON-181] Open-Closed Principle을 예시와 함께 설명해 주세요.

답변: 개방 폐쇄 원칙은 확장엔 열려 있고 변경엔 닫혀 있어야 한다는 원칙입니다.

새로운 기능 추가 시 -> 기존 코드 직접 수정 x -> 새로운 코드를 추가해 확장

ex) 할인 정책을 `if-else`로 처리하면 새로운 할인 방식이 생길 때마다 기존 메서드 수정이 필요함.

`DiscountPolicy` 같은 인터페이스를 만들고, `RateDiscountPolicy`, `FixedDiscountPolicy` 같은 구현체를 분리하면 새로운 할인 정책이 생겨도 기존 코드를 크게 수정 안하고 구현체 추가 가능

참고 자료:

---

## [COMMON-182] Liskov Substitution Principle을 예시와 함께 설명해 주세요.

답변: 리스코프 치환 원칙은 자식이 부모 타입을 대체해도 동작이 깨지지 않아야 한다는 원칙입니다.

ex) `Bird` 클래스에 `fly()` 메서드가 있고, 모든 새가 날 수 있다고 가정하면 펭귄 같이 날 수 없는 새를 넣었을 때 문제가 생김.
`Flyable` 같은 별도 인터페이스로 날 수 있는 기능을 분리하는 것이 좋음

참고 자료:

---

## [COMMON-183] Interface Segregation Principle을 예시와 함께 설명해 주세요.

답변: 인터페이스 분리 원칙은 클라이언트가 사용하지 않는 메서드에 의존하지 않도록 인터페이스를 작게 분리해야 한다는 원칙입니다.

ex) `Machine` 인터페이스에 `print()`, `scan()`, `fax()`가 모두 정의되어 있으면 프린터 입장에선 `print()`만 필요한데, `scan()`, `fax()`까지 구현해야 되면 불필요한 의존 생김
프린터, 스캐너, 팩스 인터페이스로 나눠서 각 구현체가 필요한 기능만 구현하도록 구성해서 인터페이스를 더 명확하고 영향 범위를 작게 해야 한다.

참고 자료:

---

## [COMMON-184] Dependency Injection이 무엇이고, 어떤 장점이 있는지 설명해 주세요.

답변: 의존성 주입은 객체가 필요한 의존성을 직접 생성하지 않고 외부에서 주입받는 방식입니다.

ex) 컨트롤러가 서비스를 직접 `new`로 만들지 않고, 생성자를 통해 서비스를 주입받는 것이 DI.
스프링에서 컨트롤러, 서비스, 리포지토리 등을 빈으로 등록해서 스프링 컨테이너가 필요한 의존성을 자동으로 주입함.

객체 간 결합도를 낮출 수 있고, 구현체 교체도 쉬워지고, 또 스프링은 DI를 통해서 빈을 컨테이너가 관리하니까 `@Transactional` 같은 AOP 기반 부가 기능 적용도 적합해진다. 

참고 자료:

---

## [COMMON-185] Adapter Pattern이 무엇이고, 어떤 상황에서 사용할 수 있는지 설명해 주세요.

답변: 어댑터 패턴은 서로 호환되지 않는 인터페이스를 연결해주는 구조 패턴입니다.
기존 클래스의 인터페이스가 클라이언트가 기대하는 인터페이스와 다를 때, 중간에 어댑터를 두어 사용할 수 있게 만듭니다.

예를 들어서 외부 결제 API가 `requestPayment()`라는 메서드를 제공하지만, 우리 서비스는 `pay()`라는 공통 인터페이스를 사용할 때, 외부 API를 직접 수정할 수 없다면
어댑터를 만들어 `pay()` 호출을 외부 API의 `requestPayment()` 호출로 변환할 수 있습니다.

그래서 외부 라이브러리나 레거시 시스템, 서로 다른 API를 코드 구조에 맞게 연결할 때 유용하다고 알고 있습니다.

참고 자료:

---

## [COMMON-186] Proxy Pattern이 무엇이고, 어떤 상황에서 사용할 수 있는지 설명해 주세요.

답변: 프록시 패턴은 실제 객체에 직접 접근하지 않고, 대리 객체로 접근을 제어하는 구조 패턴입니다.

프록시는 실제 객체와 같은 인터페이스를 제공하면서, 호출 전후에 추가 작업을 수행할 수 있는데요.
예를 들어 접근 권한 검사나 로깅, 캐싱, 트랜잭션 처리 같은 걸 프록시를 통해 적용할 수 있습니다.

스프링의 경우에도 AOP에서 프록시 개념을 활용합니다. 예를 들어 `@Transactional`이 붙은 서비스 메서드를 호출하면 Spring이 프록시를 통해 트랜잭션 시작과 커밋, 롤백 같은 부가 기능을 적용할 수 있습니다.

참고 자료:

---

## [COMMON-187] Decorator Pattern이 무엇이고, 상속과 비교했을 때 어떤 장점이 있는지 설명해 주세요.

답변: 데코레이터 패턴은 기존 객체를 감싸는 객체를 만들어 기능을 동적으로 추가하는 구조 패턴입니다.

상속을 사용하면 기능 조합이 많아질수록 하위 클래스가 계속 늘어날 수 있습니다.
기본 알림에 이메일, SMS, 카카오 알림 기능 같은 걸 조합할 때 상속만 사용하면 조합별 클래스를 만들어야 됩니다.

이럴 때 데코레이터 패턴을 사용하면 기본 객체를 여러 데코레이터로 감싸면서 기능을 조합할 수 있습니다.
그래서 상속보다 유연하게 기능을 확장할 수 있고, 런타임에 조합을 바꾸기도 쉽습니다.

참고 자료: [디자인패턴](https://refactoring.guru/design-patterns/structural-patterns)

---

## [COMMON-188] Facade Pattern이 무엇이고, 어떤 장점이 있는지 설명해 주세요.

답변: 퍼사드 패턴은 복잡한 서브 시스템을 단순한 인터페이스로 감싸서 사용하기 쉽게 만드는 구조 패턴입니다.

주문 생성 과정에서 재고 확인, 결제, 쿠폰 사용, 알림 발송 등 여러 하위 시스템을 호출해야 한다고 할 때, 클라이언트가 이 모든 시스템을 직접 호출하면 복잡도가 높아질 수 있습니다.
그래서 퍼사드를 두면 클라이언트는 `orderFacade.createOrder()`처럼 단순한 메서드를 호출하고, 내부 복잡한 절차는 Facade가 처리할 수 있는데요

이를 통해서 클라이어튼 코드를 단순하게 해서 복잡한 내부 구현을 숨길 수 있습니다. 또 외부에서 일관된 진입점을 사용할 수 있어서 결합도를 낮추는 데에도 도움이 됩니다.

참고 자료: [디자인패턴](https://refactoring.guru/design-patterns/structural-patterns)


---

## [COMMON-189] Template Method Pattern이 무엇인지 설명해 주세요.

답변: 템플릿 메서드 패턴은 알고리즘 전체 흐름은 부모에서 정의하고, 일부 세부 단계를 자식 클래스에서 구현하도록 하는 패턴입니다.

파일 업로드 처리 과정이 `검증 -> 저장 -> 후처리`라는 공통 흐름을 가진다고 할 때, 전체 순서는 부모 클래스에서 고정해두고, 파일 종류에 따른 검증 및 저장 방식은 자식에서 다르게 구현할 수 있습니다.

보통 비슷한 처리 흐름을 공유하면서 일부 단계만 다른 경우 유용한데, 상속 기반 패턴이라 계층 구조가 복잡해질 수 있고, 유연한 조합이 필요할 땐 전략 패턴이 더 적합할 수 있습니다.

참고 자료: [디자인패턴](https://refactoring.guru/design-patterns/structural-patterns)


---

## [COMMON-190] 이벤트 기반 아키텍처가 무엇이고, 어떤 장단점이 있는지 설명해 주세요.

답변: 이벤트 기반 아키텍처는 시스템에서 발생한 사건을 이벤트로 발행하고, 해당 이벤트에 관심 있는 컴포넌트가 이를 구독해 처리하는 구조입니다.

예를 들어 주문이 완료되면 `OrderCompletedEvent`를 발행하고, 알림 서비스, 포인트 서비스, 배송 서비스가 각각 이벤트를 받아 후속 작업을 진행할 수 있는데

이런 점에서 서비스 간 결합도를 낮추고, 비동기 처리를 통해 응답 시간 단축, 기능 확장이 가능해집니다.
다만, 이벤트 흐름이 복잡해지면 추적과 디버깅이 어려워지고, 메세지 유실, 중복 처리, 순서 보장, 일관성 문제를 고려해야 합니다.

참고 자료: https://learn.microsoft.com/en-us/azure/architecture/guide/architecture-styles/event-driven

---

## [COMMON-191] 동기 처리와 비동기 처리 중 어떤 방식을 선택할지 판단하는 기준을 설명해 주세요.

답변: 동기 처리는 요청을 보낸 쪽이 결과를 받을 때까지 기다리는 방식입니다. 예를 들어 결제 승인 결과가 있어야 주문을 확정할 수 있는 시스템의 경우 동기 처리가 적합합니다.

반면 비동기 처리는 요청을 보낸 후 결과를 기다리지 않고 다음 작업을 진행합니다. 주문 완료 후에 알림이나, 이메일, 로그 적재 같은 작업은 응답 시간 단축이 중요해서 이런 경우에 주로 활용됩니다.

결과가 즉시 필요한지, 처리 시간이 긴지, 재시도와 장애 격리가 필요한지에 따라 사용 여부를 판단합니다.

참고 자료:

---

## [COMMON-192] Monolithic Architecture와 Microservices Architecture의 차이를 설명해 주세요.

답변: 모놀리식 아키텍처는 하나의 앱 안에 여러 기능이 함께 포함되어 배포됩니다. 개발, 배포가 단순하고 트랜잭션 관리가 쉽습니다.
다만, 시스템이 커질수록 코드가 복잡해지고, 트래픽이 쏠리는 경우 비교적 대처도 어려워집니다.

MSA는 애플리케이션을 작은 서비스 단위로 나눠서 각 서비스가 독립 개발, 배포, 확장될 수 있도록하는 구조입니다. 그래서 장애 격리, 트래픽 분산에 좋습니다.

다만, 네트워크 통신이나, 분산 트랜잭션, 모니터링, 로그 추적, 배포 자동화 등 운영 복잡도가 증가합니다.
그래서 마냥 도입하면 오버헤드가 커지기 때문에 시스템 규모, 서비스 복잡도, 요구사항 등을 적절히 고려하는 게 좋습니다.

참고 자료:

---

## [COMMON-193] MSA를 도입할 때 얻는 장점과 발생할 수 있는 문제를 설명해 주세요.

답변: 서비스별 독립 배포와 확장이 가능합니다. 특정 기능에 트래픽이 쏠려도 격리 되어 있으니까 해당 서비스만 확장하면 되고, 팀별로 서비스를 나눠서 개발하기도 편합니다.
또 한 서비스의 장애가 다른 서비스의 장애로 번지지 않습니다.

다만, 서비스 간 통신이 네트워크를 통하기에 지연이나 실패 가능성도 생기고, 또 데이터가 서비스별로 분리되면 트랜잭션이나 정합성 관리가 어려워집니다.
그래서 로그 추적, 모니터링, 배포 자동화, API 버전 관리가 필요해서 복잡도가 커집니다.

참고 자료:

---

## [COMMON-194] 장애 격리와 장애 전파 방지 관점에서 시스템을 어떻게 설계할 수 있는지 설명해 주세요.

답변: 장애 격리는 한 곳의 장애가 전체로 번지지 않게 영향의 범위를 제한하는 설계입니다.

서비스나 기능 별로 자원을 분리할 수 있는데, 예를 들어 스레드 풀, 커넥션 풀, 큐, DB 같은 걸 분리하면 특정 기능의 장애로 인한 전체 서비스 자원 고갈을 줄일 수 있습니다.

또 외부 API 호출에 타임아웃이나 재시도를 설정하고, 장애가 반복되면 Circuit Breaker를 통해 빠르게 실패 처리를 할 수도 있습니다. 

비동기 메세징, 큐를 사용해서 순간 장애나 트래픽 증가 완충도 가능하겠죠.
장애 발생 시 기본값이나 데이터를 반환하는 fallback 전략도 가능하겠습니다.

결론적으로 중요한 건 실패를 완전히 없애기보단, 실패가 발생했을 때 영향을 어떻게 제한하고, 복구 가능하게 만들것인지가 중요한 것 같습니다.

참고 자료:

---

## [COMMON-195] 로깅과 모니터링이 중요한 이유를 설명해 주세요.

답변: 시스템 상태 파악 및 장애 원인 분석을 위해 중요합니다. 

로깅은 애플리케이션에서 발생한 이벤트, 요청 흐름, 에러 정보를 기록하는 것이빈다.
장애 발생 시에 어떤 요청에서 문제가 있고, 예외가 발생하는지 추적하는 데 필요합니다.

모니터링은 CPU나 메모리, 응답 시간, 에러율, 트래픽, DB 커넥션 같은 `지표를 수집하고 관찰`하는 것입니다.
장애가 발생하기 전에 이상 징후를 찾거나, 배포 후의 성능 변화를 확인하는 데 주로 사용합니다.

보통 로그, 메트릭, 트레이싱으로 어디에서 왜 무슨 병목이나 장애가 발생하는지 확인할 수 있어야 좋은 운영 환경이라고 볼 수 있습니다.

참고 자료:

---

## [COMMON-196] API 설계 시 고려해야 할 요소를 설명해 주세요.

답변: 리소스와 URI를 목적에 맞게 Restful 하게 정의해야 합니다.

메서드도 작업의 의미와 맞게 적용할 수 있어야 하고, 응답 상태 코드나 에러 응답 형식을 일관되게 설계해야 합니다.

추가적으로 인증 및 인가, 입력값 검증, 페이지네이션, 정렬, 필터링, API 버전 관리, 멱득성, Rate Limiting, 문서화 등도 고려해야 합니다.

참고 자료:

---

## [COMMON-197] 좋은 에러 처리 방식이란 무엇인지 설명해 주세요.

답변: 사용자나 클라이언트가 문제의 상황을 명확히 이해하고 대응할 수 있도록 정보를 제공하는 것이라고 생각합니다.

상황에 적절한 HTTP 상태 코드를 사용해야 하고, (ex 잘못된 요청 : 400 / 인증 실패 : 401 / 권한 없음 : 403 / 존재하지 않는 리소스 : 404 / 서버 내부 오류 : 500)
응답에 에러 코드를 포함한 적절한 메세지나, 원인을 포함하면 좋습니다.

참고 자료:

---

## [COMMON-198] 코드 리뷰에서 중점적으로 봐야 할 요소를 설명해 주세요.

답변: 코드 자체의 가독성(중복 코드 포함), 책임 분리, 결합도 등이 어느 정도인지 우선 검토하고, 성능이나 보안 이슈도 봅니다.

불필요한 반복 쿼리, 민감 정보 로그 출력, 입력값 검증 누락 같은 것들도 확인해야 되고, 나아가 네이밍이나 테스트 코드도 신경써서 최대한 클린코드로 만들면 좋습니다.

참고 자료:

---

## [COMMON-199] 유지보수하기 좋은 코드의 특징을 설명해 주세요.

답변: 가독성이 좋아야 되고 책임이 적절하게 분리돼서 변경하기 쉬운 코드라고 생각합니다.

복잡한 조건이나 긴 메서드는 적절히 분리하고 외부 의존성이나 비즈니스 로직에 너무 강하게 묶이지 않게끔, 인터페이스나 계층 분리를 하면 좋다고 봅니다.

처음부터 완벽한 코드란 없다고 생각하기 때문에 다른 사람들도 잘 이해하고 수정해 나갈 수 있는 구조의 코드가 좋다고 봅니다.

참고 자료:

---

## [COMMON-200] 프로젝트 경험을 기술면접에서 설명할 때 문제 상황, 선택 이유, 결과, 아쉬운 점을 어떻게 구조화하면 좋을지 설명해 주세요.

답변: 프로젝트 경험은 `문제 상황 - 원인 분석 - 선택지 Trade-off - 선택 이유 - 구현 과정 - 결과 - 개선점` 순서로 구조화합니다.

참고 자료:
